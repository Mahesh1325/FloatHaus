# assets/fonts

Place custom web fonts here if not using Google Fonts CDN.

Supported formats: .woff2 (preferred), .woff, .ttf
Reference them in assets/css/style.css using @font-face.

Currently using Google Fonts CDN (Inter + Playfair Display).
